{
  "name": "palcodex-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MCP server that indexes PalCodex (the Palworld modding wiki) so agents can search and read it locally.",
  "bin": {
    "palcodex-mcp": "index.mjs"
  },
  "scripts": {
    "start": "node index.mjs",
    "smoke": "node smoke.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "gray-matter": "^4.0.3",
    "minisearch": "^7.1.0",
    "zod": "^3.24.0"
  }
}
